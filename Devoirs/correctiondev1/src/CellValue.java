/**
 * An enum class that defines the
 * values <b>EMPTY</b>,  <b>X</b>
 * and <b>O</b>.
 *
 * @author Guy-Vincent Jourdan, University of Ottawa
 */

enum CellValue {
    // états; constantes
	EMPTY, X, O

}
/*private static void main ( String [] args ) {
    CellValue cellVal = CellValue.EMPTY;
    //change cellVal of X'd/O'd cell to appropriate constant 
    //depending on whose turn
    if(cellVal==X){
         
        }
    }//close main*/